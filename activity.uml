@startuml

title Activity Diagram \n


(*) --> if "Подключение к сети Интернет" then

     -->[Да] if  "Обсуждение под выбранным сообщением не было создано"
        -->[Да] "Создание обсуждения"
        --> ==CHECK1==
        --> if "Пользователь желает оставить комментарий"
        
            -->[Да] if "Альтернативные сценарии"
                --> "Набор тектового комментария"
                --> "Отправление комментария"
                if "Когда оставлен комментарий?"
                -->[До удаления обсуждения] "Отображение комментария"
                -->(*)
                else
                -->[После удаления обсуждения] "Отображение ошибки"
                -->(*)
                endif
                else
                -->[Ввод недопустимого количества символов] "Ошибка"
                --> ==CHECK1==
                else 
                --> "Отправление аудио/видеосообщения (Сценарий)"
                --> (*)
                endif
            else
            -->[Нет] if "Альтернативные сценарии"
            --> "Свернуть ветку обсуждения"
            -->(*)
            else
            --> "Удаление комментария (Сценарий)"
            -->(*)
            else
            --> "Удаление обсуждения (Сценарий)"
            -->(*)
            endif
            endif
        else 
        -->[Нет] "Развернуть ветку обсуждения"
        --> ==CHECK1==
    endif
    
     

else
    -->[Нет] (*)
endif


@enduml